/**
 * Footer link columns, in the order shown in the design. Each key maps to
 * `Footer.columns.<key>` in the messages, whose `items` is an array of labels.
 */
export const footerColumns = ["cabinet", "ressources", "outils"] as const;
